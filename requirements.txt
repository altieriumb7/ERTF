# Core ML libs (torch must match your CUDA wheel already installed)
torch>=2.0.1

# transformers must be >=4.55 for trl 0.22.2
transformers>=4.55.0,<5.0

# RL / accelerator / PEFT / bitsandbytes
accelerate>=1.4.0
trl==0.22.2
peft==0.5.0
bitsandbytes==0.40.2

# Datasets / evaluation / numeric parsing
datasets>=3.0.0
sympy==1.12
sentence-transformers==2.2.2
faiss-cpu==1.7.4

# Logging & tooling
wandb==0.15.12
huggingface_hub==0.17.1
ruamel.yaml==0.17.21
psutil

# Safety/compat with compiled extensions
numpy<2

# Extras
safetensors
tokenizers
